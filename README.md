# AIVirus.org — The Threat Model

**Data Over Denial.** The concept anchor of the Fiends Grid: addiction framed as an intelligence virus that hijacks the dopamine system. Live at [aivirus.org](https://aivirus.org). Companion products: [aafiends.com](https://aafiends.com) (daily dashboard) · [racefiends.com](https://racefiends.com) (physical arm).

## What's built
Landing page with the AIV threat model, 12-Step firewall framing, interactive 12-question diagnostic teaser, TrenchRepository (community mitigation library — submission portal marked Coming Soon until the Tally.so → Notion pipeline is wired).

## Roadmap (ecosystem runbook Phase 3)
Multi-addiction expansion ("Addiction Intelligence Virus" — 10 vectors) · `/the-virus` field manual · `/bio12` physical-recovery protocol landing page.

## Stack
Next.js (App Router) · TypeScript · Tailwind · Firebase Hosting · framer-motion

## Run it
```bash
npm install
npm run dev   # http://localhost:3000
```
No env vars required — this site is static marketing content (no auth, no database writes).

## Before deploying
```bash
bash scripts/pre-deploy-check.sh
```

## Conventions
`CLAUDE.md` (AI session rules) · `docs/PROJECT_STATUS.md` (living ledger) · no fabricated statistics · "done" = human-verified in a browser.
